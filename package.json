{
  "name": "teleschmie-de",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/check-assets.mjs && date +%s > public/build.txt",
    "build": "astro build && node scripts/glossary-linker.mjs",
    "build:verify": "npm run build && node scripts/check-links-dist.mjs",
    "preview": "astro preview",
    "lint": "astro check",
    "check-links": "node scripts/check-links.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^3.1.9",
    "@astrojs/sitemap": "^3.2.1",
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.16.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.7",
    "typescript": "^5.9.3"
  }
}
