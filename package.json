{
  "name": "teleschmie-de",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/check-assets.mjs && date +%s > public/build.txt",
    "build": "node scripts/glossary-linker.mjs && bash scripts/generate-llms-full.sh && astro build",
    "build:verify": "npm run build && node scripts/check-links-dist.mjs",
    "preview": "astro preview",
    "lint": "astro check",
    "check-links": "node scripts/check-links.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "@astrojs/sitemap": "^3.6.3",
    "@astrojs/tailwind": "^5.0.0",
    "astro": "^5.18.1",
    "tailwindcss": "^3.4.19",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "typescript": "^5.9.3"
  }
}
