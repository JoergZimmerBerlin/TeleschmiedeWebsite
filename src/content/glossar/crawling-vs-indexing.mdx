---
category: "Technisches SEO & UX"
title: 'Crawling vs. Indexing: Die Pipeline-Architektur von Google und LLMs'
description: 'Crawling ist nicht gleich Indexierung. Verstehe die mehrstufigen Pipelines von Suchmaschinen und RAG-Systemen, um De-Indexierung und Crawler-Timeouts zu verhindern.'
date: "2026-03-10"
image: "../../assets/images/glossar/3d-light/glossar-indexing-3d.webp"
image_alt: "Crawling vs Indexing 3D Infografik - Pipeline Architektur"
related_terms: ["crawler", "robots-txt", "sitemap", "llms-txt"]
key_takeaways:
  - "Zwei getrennte Systeme: Crawling ist das bloße Herunterladen der Daten (Fetch). Indexing ist das tiefgehende Parsing und die Vektorisierung für die Datenbank."
  - "Das Rendering-Gap: Zwischen Crawling und Indexing liegt oft das Rendering (JS-Ausführung), welches Tage dauern kann oder in einem Timeout endet."
  - "KI-Pipelines: Auch RAG-Systeme trennen den Fetch-Prozess (Scraper) strikt von der Einbettung in den Vektorraum (Embedding und Indexierung)."
faqs:
  - question: 'Warum wird meine Seite gecrawlt, aber nicht indexiert?'
    answer: 'Meistens wegen "Crawled - currently not indexed". Der Crawler hat die Seite gefunden, aber die Indexing-Pipeline stuft den Inhalt als nicht relevant genug ein (Thin Content), oder das asynchrone Rendering schlägt fehl.'
  - question: 'Wie steuere ich Crawling und Indexing getrennt?'
    answer: 'Crawling steuerst du primär auf Netzwerkebene über die robots.txt (Disallow) und Crawl-Budgets. Indexing steuerst du auf Dokumentebene über HTTP-Header (X-Robots-Tag) oder Meta-Robots (noindex).'
  - question: 'Verstehen KI-Crawler Meta-Robots?'
    answer: 'Teilweise. Crawler respektieren oft die robots.txt für das Fetching. Um zu verhindern, dass Systeme deine gecrawlten Daten speichern oder trainieren, solltest du strikte Header oder Opt-Out-Protokolle nutzen.'
---

Moin! 🌻

Einer der tödlichsten Denkfehler, den ich bei Entwicklern und "SEO-Experten" immer wieder sehe, ist die naive Annahme, Crawling und Indexing seien exakt dasselbe. "Der Bot war da, also bin ich im Index!" Falsch. 

Wir schreiben das Jahr 2026. Suchmaschinen und moderne Daten-Pipelines (wie RAG – Retrieval-Augmented Generation) arbeiten nicht mit einem simplen Script, das eine URL aufruft und direkt in die SQL-Datenbank klatscht. Sie nutzen gigantische, asynchrone Multi-Stage-Pipelines. Crawling und Indexing sind zwei völlig getrennte Systeme, die oft auf komplett unterschiedlichen Serverclustern laufen und durch massive zeitliche Verzögerungen (Gaps) voneinander getrennt sind.

Lass uns Tacheles reden: Wer diese Architektur nicht auf Systemebene versteht, wird niemals begreifen, warum sein massives React-Frontend zwar hunderte Crawler-Hits in den Logfiles hat, aber in der Suche komplett unsichtbar bleibt.

## Stage 1: Crawling (Der Fetch-Prozess)

Crawling ist der absolut simpelste Teil der Gleichung. Es ist im Grunde ein hochskalierter `HTTP GET` Request, gesteuert von einer Queue. 

Der [Crawler](/glossar/crawler/) zieht sich URLs aus seiner Frontier (der Warteschlange), löst das DNS auf, macht einen TLS-Handshake und lädt den initialen Response-Body (meist HTML oder Markdown) herunter. 

**Das Ziel des Crawlings:**
* Datenbeschaffung mit absolut minimaler Latenz.
* Extraktion neuer URLs, um die nächste Crawl-Schleife zu füttern.
* Status-Code-Prüfung (200 OK, 301 Redirect, 404 Not Found).

**Wichtig:** Ein Crawler führt in dieser isolierten Millisekunde **kein** JavaScript aus. Er liest nicht dein verschachteltes CSS. Er bewertet nicht die Qualität deines Textes. Er speichert den rohen Payload in einem gigantischen Zwischenspeicher (dem Processing-Queue) ab und verschwindet sofort wieder, um Crawl-Budget zu sparen. 

## The Gap: Parsing und Rendering

Jetzt kommt der Schritt, an dem ein Großteil der modernen Web-Apps scheitert. Der rohe Payload aus dem Crawling wird asynchron an den **Parser** übergeben.

Wenn der Parser sauberes, serverseitiges HTML oder Markdown sieht, jubelt das System. Die Daten sind sofort lesbar und gehen direkt in die Indexierungs-Phase.
Wenn der Parser aber feststellt, dass die Seite primär aus einem leeren `<div id="root"></div>` besteht und massiv JavaScript benötigt (Client-Side Rendering), stoppt die Pipeline hart.

Die Seite wird in die **Render-Queue** verschoben. Hier warten Headless-Browser (sogenannte Web Rendering Services - WRS) darauf, freie Rechenkapazitäten zu haben, um dein JavaScript auszuführen (Hydration) und das finale DOM zu generieren. 

* **Das Nadelöhr:** Rendering kostet Tech-Giganten ein Vermögen an Rechenleistung. Die Render-Queue kann extrem lang sein. Es kann Tage dauern, bis dein JavaScript gerendert wird. Wenn der WRS dann bei der Ausführung in einen Timeout läuft (z.B. wegen eines miesen [LCP](/glossar/core-web-vitals/) oder Endlos-Schleifen im Main-Thread), bricht das Rendering ab. Das Resultat? Du bist gecrawlt, aber du wirst niemals indexiert.

## Stage 2: Indexing (Bewertung und Speicherung)

Erst wenn das final geparste (oder erfolgreich gerenderte) DOM vorliegt, beginnt das tatsächliche Indexing. Und auch hier ist die Aufnahme in die Datenbank kein Automatismus.

Hier wird die Qualität des Inhalts algorithmisch bewertet:
1. **Kanonisierung:** Ist dieser Inhalt ein Duplikat? Gibt es eine stärkere URL mit gleichem Content? Der Algorithmus wählt den primären Knotenpunkt (Canonical).
2. **Entity Extraction:** Welche Entitäten (Personen, Orte, Konzepte) tauchen im Text auf?
3. **Semantic Mapping / Embedding:** Der Text wird analysiert und (in modernen KI-Systemen) in mathematische Vektoren umgewandelt.

Nur wenn der Algorithmus entscheidet, dass dein Inhalt besser, relevanter oder informativer ist als die Milliarden anderen Dokumente im Index, erhält deine URL den begehrten Eintrag in der Hauptdatenbank. 

Dieses selektive Phänomen siehst du in der Search Console unter der Fehlermeldung: *"Gecrawlt – zurzeit nicht indexiert" (Crawled - currently not indexed)*. Das bedeutet klipp und klar: Stage 1 (Crawling) war erfolgreich. Stage 2 (Indexing) hat deine Seite in den Mülleimer geworfen. Oft liegt das an *Thin Content* (zu wenig Mehrwert), kaputtem JavaScript-Rendering oder einem grauenhaften [Quality Score](/glossar/cpc/).

## Die Pipeline in LLMs und KI-Agenten

Moderne KI-Architekturen (wie die Datenbeschaffung für OpenAI, Perplexity oder RAG-Modelle) nutzen exakt die gleiche logische Trennung, nur noch rigoroser.

### Der RAG-Crawler (Fetch)
Der autonome Agent sucht das Web nach Fakten ab. Er nutzt APIs oder asynchrone Fetch-Routinen, um die Datenquellen abzugreifen. Diese Crawler hassen JavaScript zutiefst und lieben strukturierte Formate wie Markdown oder saubere `llms.txt` Dateien. Wenn sie an JS scheitern, brechen sie oft sofort ab, ohne es in eine Render-Queue zu schieben.

### Die Vector-Database (Indexing)
Der gecrawlte Text wird tokenisiert und per Embedding-Modell in einen multidimensionalen Vektorraum übersetzt. Dieser Schritt ist das eigentliche "Indexing" für LLMs. 

Wenn dein Text voller Layout-Shifts, Cookie-Banner-Code oder irrelevanten Navigationselementen ist (weil du kein semantisches HTML5 genutzt hast), wird dein Embedding "noisy" (verrauscht). Das bedeutet: Der mathematische Vektor, der deinen Text repräsentiert, landet im falschen semantischen Raum. Das LLM hat dich gecrawlt und technisch indexiert, aber wenn ein Nutzer eine Frage stellt, wird dein Vektor nicht als Relevanz abgerufen.

## Steuerung: So übernimmst du die Kontrolle

Du musst aufhören, diese getrennten Systeme dem Zufall zu überlassen. Du steuerst die Pipeline gezielt auf zwei Ebenen:

**Crawling blockieren (Netzwerkebene):** 
Nutze die `robots.txt` (`Disallow: /admin/`). Der Crawler bricht den Fetch-Versuch auf Netzwerkebene ab. Es kostet kein Crawl-Budget. **Achtung:** Die URL kann theoretisch als reiner Link (ohne Inhalt) im Index landen, wenn externe Seiten massiv darauf verweisen.

**Indexing blockieren (Dokumentebene):**
Nutze den HTTP-Header `X-Robots-Tag: noindex` (am besten) oder das `<meta name="robots" content="noindex">` Tag im HTML-Head. Der Crawler **muss** die Seite laden (Crawl-Budget wird verbraucht!), aber der Parser in Stage 2 verwirft sie sofort und blockiert das Indexing garantiert.

Als technischer [SEO Freelancer für Berlin](/seo-freelancer-berlin/) ist mein erster Blick bei einem Projekt immer auf die Trennschärfe dieser Systeme gerichtet. Trenne Crawling von Indexing. Setze Server-Side Rendering (SSR) ein, um die tödliche Render-Queue zu umgehen. Kontrolliere deine Pipelines.

ALOHA! 🌻

---

<div class="blog-cta-box">
  <h3 class="text-2xl font-bold mb-4">Gecrawlt, aber nicht indexiert?</h3>
  <p class="mb-6">Deine Seiten hängen im "Crawled - not indexed" Limbo fest? Ich analysiere deine JavaScript-Architektur und Render-Routinen, behebe Timeouts im WRS und sorge dafür, dass deine Inhalte erfolgreich die Indexierungs-Phase erreichen.</p>
  <a href="/glossar/seo-audit/" class="btn-primary inline-flex">Jetzt Tech-Audit anfragen</a>
</div>

* [Wie Crawler arbeiten](/glossar/crawler/)
* [Was bedeutet Linkjuice?](/glossar/linkjuice/)
* [Die llms.txt erklärt](/glossar/llms-txt/)juice/)
* [Die llms.txt erklärt](/glossar/llms-txt/)
