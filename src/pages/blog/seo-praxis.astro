---
import { Image } from 'astro:assets';
import Layout from '../../layouts/Layout.astro';
import Schema from '../../components/Schema.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('blog');
const categoryPosts = posts.filter(post => {
  const postCat = post.data.category || 'SEO Praxis';
  return postCat === 'SEO Praxis';
}).sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());

const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<Layout title="SEO Praxis | Jörg Zimmer (2026)" description="Tiefgründige Case Studies, bewährte SEO-Strategien und ehrliche Insights aus 25 Jahren Performance Marketing. Echtes Wissen fürs Ranking. (2026)">
  <section class="section pt-12 pb-12 bg-white">
    <div class="container-narrow">
      <div class="mb-10">
        <a href={`${baseUrl}/blog/`} class="text-sm font-bold text-gray-muted hover:text-lime-600 transition-colors inline-flex items-center gap-2 mb-6" title="Zurück zur Blog-Übersicht (2026)">
          <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" width="16" height="16">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
          </svg>
          Zurück zur Blog-Übersicht
        </a>
        <p class="subheadline mb-2">Themen-Hub</p>
        <h1 class="text-4xl md:text-5xl font-bold leading-normal mb-4">
          <span class="highlight">SEO Praxis</span>
        </h1>
        <p class="text-lg text-gray-muted max-w-2xl">
          Tiefgründige Case Studies, bewährte SEO-Strategien und ehrliche Insights aus 25 Jahren Performance Marketing. Echtes Wissen fürs Ranking. (2026)
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        {categoryPosts.map((p) => (
          <article class="bg-white border border-gray-100 rounded-2xl overflow-hidden hover:shadow-lg hover:border-lime-accent transition-all duration-300 flex flex-col h-full group">
            <a href={`${baseUrl}/blog/${p.id}/`} class="flex flex-col h-full">
              {p.data.image && (
                <div class="overflow-hidden aspect-video">
                  <Image
                    src={p.data.image!}
                    alt={p.data.image_alt || p.data.title}
                    width={600}
                    class="w-full h-full object-cover object-top group-hover:scale-105 transition-transform duration-500"
                    format="webp"
                    loading="lazy"
                  />
                </div>
              )}
              <div class="p-6 md:p-8 flex flex-col flex-grow">
                <div class="flex items-center gap-3 mb-3">
                  <span class="text-[10px] text-gray-muted">
                    {new Date(p.data.date).toLocaleDateString('de-DE', { day: 'numeric', month: 'long', year: 'numeric' })}
                  </span>
                </div>
                <h2 class="text-xl font-bold mb-3 group-hover:text-lime-600 transition-colors">
                  {p.data.title}
                </h2>
                <p class="text-gray-muted text-sm mb-6 line-clamp-3 flex-grow">
                  {p.data.description}
                </p>
                <div class="mt-auto pt-4 border-t border-gray-50">
                  <span class="text-xs font-bold text-dark inline-flex items-center gap-1 group-hover:gap-2 transition-all">
                    Artikel lesen
                    <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                    </svg>
                  </span>
                </div>
              </div>
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>
  <Schema title="SEO Praxis | Jörg Zimmer" description="Tiefgründige Case Studies, bewährte SEO-Strategien und ehrliche Insights aus 25 Jahren Performance Marketing. Echtes Wissen fürs Ranking. (2026)" />
</Layout>
